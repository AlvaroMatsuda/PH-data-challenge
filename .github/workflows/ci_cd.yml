name: Local CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-local:
    # runs-on: ubuntu-latest
    # If you're using your own desktop runner:
    runs-on: self-hosted

    steps:
      # ---- Checkout code ----
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---- Set up Python for testing ----
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # ---- Install dependencies and run tests ----
      - name: Install dependencies
        run: |
          pip install -r app/requirements.txt

      # ---- Build Docker image locally ----
      - name: Build Docker image
        run: |
          docker build -t phdata-api:latest -f ./Dockerfile .

      # # ---- (Optional) Run the container to verify it works ----
      # - name: Run container test
      #   run: |
      #     docker run -d -p 8000:8000 --name ml-api-test ml-api:latest
      #     sleep 5
      #     docker ps
      #     docker logs ml-api-test

      # # ---- (Optional) Stop and remove the container ----
      # - name: Cleanup
      #   run: |
      #     docker stop ml-api-test
      #     docker rm ml-api-test
